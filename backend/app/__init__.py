"""Clinical calculator API.

Importing the package sets the import path to the extractor, so the evaluator
the extractor validates with is the evaluator the API serves with.
"""

from . import config as config  # noqa: F401  (import for its side effect)
